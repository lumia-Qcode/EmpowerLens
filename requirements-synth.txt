# Synthetic-data generation pipeline (src/synth). Separate from the frozen
# requirements.txt and from requirements-transformer.txt.
#   venv\Scripts\python.exe -m pip install -r requirements-synth.txt
anthropic>=0.69          # generator (Claude Sonnet)
google-genai>=1.0        # critic panel (Gemini)
# Dedup is dependency-free (token Jaccard). For optional semantic dedup later,
# add: sentence-transformers  (pulls torch — set KMP_DUPLICATE_LIB_OK=TRUE on Windows).
#
# Keys: ANTHROPIC_API_KEY (generator) + GEMINI_API_KEY or GOOGLE_API_KEY (critics).
